<div class = "jumbotron">

  <h1>Bienvenue sur Event Brite Light</h1>
		<h4>Le site de réservation de tes plus folles soirées !</h4>
		</br>
			<h6>Si tu es organisateur d'événements, tu peux en créer un <a> <%= link_to "ici !", new_event_path %> </a> </h6>
</div>


 <% if flash[:success] %>
      <div class="alert alert-success" role="alert"><%= flash[:success] %></div>
 <% elsif flash[:danger] %>
      <div class="alert alert-danger" role="alert"><%= flash[:danger] %></div>
   <% end %>

    <h4 class="h1-responsive font-weight-bold my-5">Voici les événements en stock, régale-toi...</h4>


<section>
<div class="card-deck">
<%Event.all.each do |event|%>
<div class="col-sm-4 mb-3">
<div class="card">
  <div class="card-header">
    <%=event.start_date%>
  </div>
  <div class="card-body align-center">
    <blockquote class="blockquote mb-0">
      <p><%=event.title%></p>
      <footer class="blockquote-footer">
      <div class="row justify-content-between align-self-center mx-3">
        <p><%=event.price%> €</p>
        <a class="btn btn-info" <%= link_to "Voir plus", event_path(event.id)%></a>
      </div>
      </footer>
    </blockquote>
  </div>
</div>
</div>
<%end%>
</div>
</section>
